name: update-typescript-docs
inputs:
  docs-input-path:
    required: true
# output:
#   generatedDocs:
#     value: ${{ insert here }}
runs:
  using: "composite"
  steps:
    # - run: yarn add typedoc typedoc-plugin-markdown typescript
    #   shell: bash
    # - run: yarn typedoc
    #   shell: bash
    - run: tree .
      shell: bash
