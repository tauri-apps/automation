name: update-typescript-docs
inputs:
  docs-input-path:
    required: true
# output:
#   generatedDocs:
#     value: ${{ insert here }}
runs:
  using: "composite"
  steps:
    - run: echo Path ${{ inputs.docs-input-path }}
      shell: bash
    - run: tree .
      shell: bash
    - run: tree ${{ inputs.docs-input-path }}
      shell: bash